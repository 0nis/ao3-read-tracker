export enum CollapseMode {
  GENTLE = "gentle",
  AGGRESSIVE = "aggressive",
}

export enum ButtonAction {
  TOGGLE = "toggle",
  CLICK = "click",
}

export enum LoaderType {
  SPINNER = "spinner",
  PROGRESS = "progress",
}
