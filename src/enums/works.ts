export enum WorkState {
  READ = "read",
  IN_PROGRESS = "in_progress",
  IGNORED = "ignored",
}

export enum ReadingStatus {
  ACTIVE = "active",
  WAITING = "waiting",
  PAUSED = "paused",
}

export enum FinishedStatus {
  COMPLETED = "completed",
  ABANDONED = "abandoned",
}
