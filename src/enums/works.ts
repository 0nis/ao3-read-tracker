export enum WorkState {
  FINISHED = "finished",
  IN_PROGRESS = "in_progress",
  IGNORED = "ignored",
}

export enum ReadingStatus {
  ACTIVE = "active",
  WAITING = "waiting",
  PAUSED = "paused",
}

export enum FinishedStatus {
  COMPLETED = "completed",
  DROPPED = "dropped",
  DORMANT = "dormant",
}
