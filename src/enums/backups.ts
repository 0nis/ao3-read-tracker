export enum BackupProviderType {
  GOOGLE_DRIVE = "google-drive",
}

export enum RemoteTargetKind {
  FOLDER = "folder",
  PATH = "path",
  APP_DATA_FOLDER = "app-data-folder",
  BUCKET = "bucket",
}
