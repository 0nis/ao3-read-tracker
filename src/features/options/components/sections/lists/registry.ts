import { SectionId } from "../../../sections";
import { ListSectionElements } from "./base";

/**
 * Stores extra handles for all list sections.
 */
export const listSectionRegistry = new Map<SectionId, ListSectionElements>();
